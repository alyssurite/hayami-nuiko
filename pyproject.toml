[tool.poetry]
name = "Hayami Nuiko Telegram bot"
version = "6.0"
description = "This is a bot for beautifying pixiv and twitter links in order to get clean links without any URI query garbage."
authors = ["Alyss Mesh <alyssurite@yandex.ru>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
python-telegram-bot = {version = "^22.8", extras = ["job-queue", "webhooks"]}
pyrogram = "^2.0.106"
tgcrypto = "^1.2.5"
uvloop = {version = "^0.22.1", platform = "linux"}
sqlalchemy = "^2.0.51"
alembic = "^1.18.5"
tzdata = "^2026.3"
python-magic = {version = "^0.4.27", platform = "linux"}
python-magic-bin = {version = "^0.4.14", platform = "win32"}
tenacity = "^9.1.4"
beautifulsoup4 = "^4.15.0"
logtail-python = "^0.4.0"
pixivpy-async = "^1.2.14"
aiocache = "^0.12.3"
orjson = "^3.11.9"
python-dateutil = "^2.9.0.post0"
gallery-dl = "^1.32.9"
psycopg2-binary = "^2.9.12"
fastapi = "^0.141.1"
uvicorn = "^0.52.1"
dataclasses-json = "^0.6.7"
pydantic-settings = "^2.14.2"
structlog = "^26.1.0"

[tool.poetry.group.dev.dependencies]
pre-commit = "^4.6.1"

[tool.isort]
profile = "black"
skip = ["alembic.ini"]
skip_glob = [".*/", ".*", "log/", "*.log"]
line_length = 90
include_trailing_comma = true
force_grid_wrap = 0
use_parentheses = true
ensure_newline_before_comments = true
lines_between_types = 1

[tool.black]
line-length = 90
include = '\.pyi?$'


[tool.ruff]
lint.select = ["E", "F", "B"]
lint.ignore = []

# Exclude a variety of commonly ignored directories.
exclude = [
    ".bzr",
    ".direnv",
    ".eggs",
    ".git",
    ".hg",
    ".mypy_cache",
    ".nox",
    ".pants.d",
    ".pytype",
    ".ruff_cache",
    ".svn",
    ".tox",
    ".venv",
    "__pypackages__",
    "_build",
    "buck-out",
    "build",
    "dist",
    "node_modules",
    "venv",
]
lint.per-file-ignores = {}

# Same as Black.
line-length = 90

# Allow unused variables when underscore-prefixed.
lint.dummy-variable-rgx = "^(_+|(_+[a-zA-Z0-9_]*[a-zA-Z0-9]+?))$"

# Assume Python 3.12.
target-version = "py312"
